<h1>All Songs</h1>
<table>
  <tbody>
    <% @songs.each do |song| %>
    <tr><%= link_to song.title, songs_path(song) %></tr>
    <% end %>
  </tbody>
</table>
<%= link_to "Add New Song", new_song_path %>
